class InputManager {
  // If is a react or game then bind key to different, destroy key events when event change??

  keyBindList: Array<{}>;
  constructor() {}
  destroyAllBinding = () => {};
  bindReactKey = (key, event) => {};
}
export const inputManager = new InputManager();
